apiVersion: apps/v1
kind: Deployment
metadata:
  name: reactor-server
spec:
  replicas: 4
  selector:
    matchLabels:
      app: reactor-server
  template:
    metadata:
      labels:
        app: reactor-server
    spec:
      containers:
        - args:
            - ping.ReactorServer
          image: grpc-load-balancing-demo
          imagePullPolicy: Never
          name: reactor-server
---
apiVersion: v1
kind: Service
metadata:
  name: reactor-server
spec:
  ports:
    - name: grpc
      port: 50505
  selector:
    app: reactor-server
